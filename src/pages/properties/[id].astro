---
import PropertyFeaturesSection from "@components/sections/properties/PropertyFeaturesSection.astro";
import PropertyGallerySection from "@components/sections/properties/PropertyGallerySection.astro";
import MainLayout from "@layouts/MainLayout.astro";

import main1 from '@images/properties/main-house-1.jpeg';
import main2 from '@images/properties/main-house-2.jpg';
import main3 from '@images/properties/main-house-3.jpg';
import main4 from '@images/properties/main-house-4.jpg';
import main5 from '@images/properties/main-house-5.jpg';
import LocationMapSection from "@/components/sections/properties/LocationMapSection.astro";
import ContactFormSection from "@/components/sections/properties/ContactFormSection.astro";

import realtor from '@images/claudia-1.jpg';
import { Image } from "astro:assets";

// Id de la ruta
const { id } = Astro.params;

const response = await fetch(`http://localhost:3000/api/v1/properties/constructions/${id}`);
const propertyTest = await response.json();

if (!propertyTest) {
  return new Response(null, { status: 404, statusText: 'Propiedad no encontrada' });
}

const property = {
  id: 1,
  title: "Tu próxima propiedad te espera",
  price: 17854000,
  currency: "MXN",
  type: "Venta",
  description: "Hermosa casa moderna con acabados de lujo, ubicada en una de las zonas más exclusivas de la ciudad...",
  features: {
    bedrooms: 5,
    bathrooms: 9,
    area: 618,
    landArea: 1500,
    floors: 2
  },
  images: [
    main1,
    main2,
    main3,
    main4,
    main5
  ],
  isFeatured: true,
  location: {
    address: "Rancho San Salvador",
    coordinates: {
      lat: 19.4326,
      lng: -99.1332
    }
  },
  amenities: [
    "Alberca",
    "Gimnasio",
    "Jardín",
    "Estacionamiento",
    "Seguridad 24/7"
  ],
  agent: {
    name: "Claudia de la Luna",
    phone: "+52 228 108 1122",
    email: "claudia@cimabienesraices.com",
    photo: realtor
  }
};
---

<MainLayout title="Detalles de propiedad">
  <!-- Gallery Section -->
  <PropertyGallerySection images={property.images} isFeatured={property.isFeatured} />
  <main class="max-w-7xl mx-auto px-4 py-8">
    <!-- Basic Information -->
    <div class="mt-8">
      <div class="flex justify-between items-start">
        <div>
          <h1 class="text-3xl font-bold text-gray-900">{property.title}</h1>
          <p class="text-7xl font-bold mt-2 text-green-700">
            ${new Intl.NumberFormat('es-MX').format(property.price)} {property.currency}
          </p>
        </div>
        <span class={`px-4 py-2 rounded-full font-semibold ${
          property.type === 'Venta' ? 'bg-blue-100 text-blue-800' : 'bg-green-100 text-green-800'
        }`}>
          {property.type}
        </span>
      </div>
      <p class="mt-4 text-gray-600">{property.description}</p>
    </div>

    <!-- Features -->
    <PropertyFeaturesSection {...property.features} />

    <!-- Location -->
    <LocationMapSection coordinates={property.location.coordinates} />

    <!-- Amenities -->
    <div class="mt-12">
      <h2 class="text-2xl font-semibold mb-4">Servicios y amenidades</h2>
      <div class="grid grid-cols-2 md:grid-cols-3 gap-4">
        {property.amenities.map((amenity) => (
          <div class="flex items-center gap-2 text-3xl">
            <i class="ri-checkbox-circle-fill text-brand"></i>
            <span>{amenity}</span>
          </div>
        ))}
      </div>
    </div>

    <!-- Contact Section -->
    I'll redesign the contact form and agent information section with a background image and a dark overlay to improve visibility. I'll use HTML and TailwindCSS without React, as requested. Here's an improved version of your component:

```html
<!-- Contact Section -->
<div class="mt-12 relative bg-cover bg-center py-16">
  <div class="relative z-10 container mx-auto px-4">
    <div class="grid md:grid-cols-2 gap-8">
      <!-- Contact Form -->
      <form class="space-y-6 bg-white bg-opacity-90 p-8 rounded-lg shadow-xl backdrop-blur-sm">
        <h3 class="text-3xl font-bold mb-6 text-gray-800">Contactar agente</h3>
        
        <div>
          <label for="name" class="block text-sm font-medium text-gray-700">Nombre</label>
          <input type="text" id="name" name="name" required 
                 class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-brand focus:ring-brand">
        </div>

        <div>
          <label for="email" class="block text-sm font-medium text-gray-700">Correo electrónico</label>
          <input type="email" id="email" name="email" required 
                 class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-brand focus:ring-brand">
        </div>

        <div>
          <label for="phone" class="block text-sm font-medium text-gray-700">Teléfono</label>
          <input type="tel" id="phone" name="phone" required 
                 class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-brand focus:ring-brand">
        </div>

        <div>
          <label for="message" class="block text-sm font-medium text-gray-700">Mensaje</label>
          <textarea id="message" name="message" rows="4" required
                    class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-brand focus:ring-brand"></textarea>
        </div>

        <button type="submit" 
                class="w-full bg-brand-500 hover:bg-brand-600 text-white py-3 px-6 rounded-lg transition-colors duration-300 ease-in-out transform hover:scale-105">
          Enviar mensaje
        </button>
      </form>
      
      <!-- Agent Information -->
      <div class="bg-white bg-opacity-90 p-8 rounded-lg shadow-xl backdrop-blur-sm">
        <div class="flex items-center gap-4 mb-6">
          <Image
            src={property.agent.photo}
            alt={property.agent.name}
            class="w-48 h-48 rounded-full object-cover border-4 border-brand-500 shadow-lg"
          />
          <div>
            <h3 class="text-2xl font-bold text-gray-800">{property.agent.name}</h3>
            <p class="text-brand-600 font-semibold">Agente Inmobiliario</p>
          </div>
        </div>
        <div class="space-y-4">
          <p class="flex items-center gap-3 text-gray-700">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 text-brand-500" viewBox="0 0 20 20" fill="currentColor">
              <path d="M2 3a1 1 0 011-1h2.153a1 1 0 01.986.836l.74 4.435a1 1 0 01-.54 1.06l-1.548.773a11.037 11.037 0 006.105 6.105l.774-1.548a1 1 0 011.059-.54l4.435.74a1 1 0 01.836.986V17a1 1 0 01-1 1h-2C7.82 18 2 12.18 2 5V3z" />
            </svg>
            <span class="text-lg">{property.agent.phone}</span>
          </p>
          <p class="flex items-center gap-3 text-gray-700">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 text-brand-500" viewBox="0 0 20 20" fill="currentColor">
              <path d="M2.003 5.884L10 9.882l7.997-3.998A2 2 0 0016 4H4a2 2 0 00-1.997 1.884z" />
              <path d="M18 8.118l-8 4-8-4V14a2 2 0 002 2h12a2 2 0 002-2V8.118z" />
            </svg>
            <span class="text-lg">{property.agent.email}</span>
          </p>
        </div>
      </div>
    </div>
  </div>
</div>
  </main>
</MainLayout>
